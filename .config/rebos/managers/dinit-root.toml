# Dinit (root)

add = "doas dinitctl enable #:? || exit 0"
remove = "doas dinitctl disable #:? || exit 0"

plural_name = "dinit services (root)"
hook_name = "dinit_services_root"

[config]
many_args = true
