layout {
	background-color "{background}"
	border {
        active-color "{accent}"
        inactive-color "{color8}"
        urgent-color "{color1}"
    }
}

overview {
	backdrop-color "{background}"
}

recent-windows {
	highlight {
		active-color "{accent}"
		urgent-color "{color1}"
	}
}
