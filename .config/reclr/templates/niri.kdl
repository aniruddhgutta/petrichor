layout {
	background-color "{background}"
	focus-ring {
        active-color "{accent}"
        inactive-color "{color8}"
    }
}

overview {
	backdrop-color "{background}"
}

recent-windows {
	highlight {
		active-color "{accent}"
		urgent-color "{color1}"
	}
}
