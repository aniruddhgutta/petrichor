// global: rounded corners for all windows
window-rule {
    geometry-corner-radius 3
    clip-to-geometry true
    draw-border-with-background false
}

// launcher + waybar: shadow and blur at startup
layer-rule {
    match namespace="^(launcher|waybar)$"

    shadow { on; }
    background-effect {
        blur true
        xray false
    }
    geometry-corner-radius 0
}

// place overview backdrop behind everything
layer-rule {
    match namespace="^awww-daemonbackdrop$"
    place-within-backdrop true
}

// block screencast-sensitive windows and layers
window-rule {
    match app-id=r#"^org\.keepassxc\.KeePassXC$"#
    match app-id=r#"^org\.gnome\.World\.Secrets$"#
    match app-id=r#"^org\.telegram\.desktop$"#
    match app-id=r#"^org\.gnome\.Fractal$"#

    block-out-from "screencast"
}
layer-rule {
    match namespace="^notifications$"
    block-out-from "screencast"
}

// blur windows
window-rule {
    match app-id=r#"^spotify$"#

    opacity 0.9
    background-effect { blur true; }
}

// non-xray blur (suitable for floating windows, popups and shells)
window-rule {
    match app-id=r#"^foot$"# title="^impala$"
    match app-id=r#"^foot$"# title="^bluetuith$"
    match app-id=r#"^foot$"# title="^wiremix$"
    match app-id=r#"^foot$"# title="^btop$"

    shadow { on; }
    opacity 0.9
    background-effect {
        blur true
        xray false
    }
}

// use 2/3 width column
window-rule {
    match app-id=r#"^com\.discordapp\.Discord$"#
    match app-id=r#"^spotify$"#
    match app-id=r#"^com\.usebottles\.bottles$"#
    match app-id=r#"^org\.gnome\.Loupe"#

    default-column-width { proportion 0.66667; }
}

// use full width column
window-rule {
    match app-id="^obsidian$"
    match app-id=r#"^dev\.zed\.Zed$"#
    match app-id=r#"^spotify$"#
    match app-id=r#"^org\.gimp\.GIMP$"#
    match app-id=r#"^foot$"# title="^spotify_player$"

    open-maximized true
}

// use fullscreen
window-rule {
    match app-id=r#"^page\.kramo\.Cartridges$"#
    match app-id=r#"^com\.libretro\.RetroArch$"#
    match app-id=r#"^blender$"#
    match app-id=r#"^Minecraft"#
    open-fullscreen true
}

// open floating and use their own preferred size
window-rule {
    match app-id=r#"firefox$"# title="^Picture-in-Picture$"
    match app-id=r#"^org\.gnome\.Calculator"#
    match app-id=r#"^org\.gnome\.Decibels"#
    match app-id=r#"^org\.gnome\.NautilusPreviewer$"#
    match app-id=r#"^org\.gnome\.Showtime$"#
    match app-id=r#"^app\.drey\.Warp"#
    match app-id=r#"^org\.telegram\.desktop$"# title="^Media viewer$"
    match app-id=r#"^org\.prismlauncher\.PrismLauncher$"# title="^Console window"
    match app-id=r#"^foot$"# title="^impala$"
    match app-id=r#"^foot$"# title="^bluetuith$"
    match app-id=r#"^foot$"# title="^wiremix$"
    match app-id=r#"^foot$"# title="^btop$"
    match app-id=r#"^foot$"# title="^rmpc$"
    match app-id=r#"^foot$"# title="^wallpaper-menu$"
    match app-id=r#"^resolve$"# title="^resolve$"
    match app-id=r#"^resolve$"# title="^Create New Project$"
    match app-id=r#"^resolve$"# title="^Project Media Location$"
    match app-id=r#"^resolve$"# title="^Import Media$"
    match app-id=r#"^resolve$"# title="^Problem Report"
    match app-id=r#"^org.kde.kdenlive$"# title="^Kdenlive$"
    match app-id=r#"^blender$"# title="^Preferences"
    match app-id=r#"^wineboot.exe$"# title="^Wine$"
    match app-id=r#"Preferences"# title="^Preferences"
    match title="^Open Files$"

    open-floating true
    default-column-width {}
    default-window-height {}
}

// csd-friendly presentation
window-rule {
    match app-id=r#"^org\.gnome\."#    // all gnome apps
    match app-id=r#"^app\.drey\.Warp"#

    tiled-state false
    geometry-corner-radius 15
}

// WezTerm: empty default-column-width works around initial configure bug
window-rule {
    match app-id=r#"^org\.wezfurlong\.wezterm$"#
    default-column-width {}
}

// Steam: non-main windows as floating, toast notifications pinned bottom-right
window-rule {
    match app-id="steam"
    exclude title=r#"^[Ss]team$"#
    open-floating true
    default-column-width {}
    default-window-height {}
}
window-rule {
    match app-id="steam" title=r#"^notificationtoasts_\d+_desktop$"#
    default-floating-position x=10 y=10 relative-to="bottom-right"
    open-focused false
}

// GIMP: don't focus the startup splash
window-rule {
    match app-id=r#"^org\.gimp\.GIMP$"# title=r#"^GIMP Startup$"#
    open-focused false
}
window-rule {
    match app-id=r#"^org\.gimp\.GIMP$"# title=r#"^Preferences$"#
    open-floating true
}

// KeePassXC: always focus unlock/access dialogs
window-rule {
    match app-id=r#"^org\.keepassxc\.KeePassXC$"# title="^KeePassXC - Browser Access Request$"
    match app-id=r#"^org\.keepassxc\.KeePassXC$"# title="^Unlock Database - KeePassXC$"

    open-focused true
}

// wine: let it determine its own size
window-rule {
    match app-id="^explorer.exe$"
    default-column-width {}
}
