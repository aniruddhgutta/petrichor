alias  ..="cd .."
alias ...="cd ../.."

alias la="ls -lAh --color"  # not posix-compliant
alias ls="ls -Ah --color"   # not posix-compliant

alias rm="rm -v"  # not posix-compliant
alias rf="rm -rf"
alias cp="cp -i"

alias df="df -Th"
alias  x="xdg-open"
alias vi="$EDITOR"

alias  s="$(command -v doas || command -v ssu  || command -v sudo) --"
alias se="$(command -v doasedit || printf "s %s" $EDITOR) --"

alias mi="s make clean install"
alias mc="make clean"
alias diff="diff -u --color"  # not posix-compliant

alias start_aria='aria2c --conf-path="${XDG_CONFIG_HOME:-$HOME/.config}/aria2/aria2.conf" -D'
alias a2p="aria2p --secret 'aria'"
alias close_aria="pkill -f aria2c"

alias dots='/usr/bin/git -C $DOTS_DIR'  # alias to dots repo

alias gs="git status --short"
alias gd="git diff --output-indicator-new=' ' --output-indicator-old=' '"
alias gl='git log --graph --all --pretty=format:"%C(magenta)%h %C(white) %an  %ar%C(auto)  %D%n%s%n"'

alias ga="git add"
alias gc="git commit"
alias gp="git push"
alias gu="git pull"

alias gb="git branch"
alias gi="git init"
alias gcl="git clone"
