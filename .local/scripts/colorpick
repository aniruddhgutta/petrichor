#!/bin/sh
# Color picker using niri

output=$(niri msg pick-color)
color=${output##*Hex: }

[ "$color" ] && {
    printf "%s" "$color" | wl-copy
    printf "%s\n" "$color"
    notify-send "Picked color" \
        "<span color='$color'></span> $color copied to clipboard"
}
